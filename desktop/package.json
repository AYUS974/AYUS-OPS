{
  "name": "ayus-desktop",
  "version": "1.0.0",
  "description": "AYUS — JARVIS-style desktop assistant. Electron shell over the ayus-ops server + web app.",
  "main": "main.js",
  "scripts": {
    "start": "node run.js",
    "build:web": "npm run build --prefix .."
  },
  "devDependencies": {
    "electron": "^33.2.0"
  }
}
